# Observations
<!-- Append-only. Format: - YYYY-MM-DD [tags]: observation -->
<!-- ≤4 lines per entry. Anything longer is a spec — link to a doc instead. Skip status updates. -->
<!-- Tags: progress, blocker, insight, decision, meta, failed, anti-pattern -->
<!-- failed = a specific attempt that didn't work (retrospective); anti-pattern = distilled negative rule from one or more failures. Distinct from blocker (something stopping progress now). -->
