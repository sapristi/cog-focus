# Observations
<!-- Append-only. Format: - YYYY-MM-DD [tags]: observation -->
<!-- Tags: progress, blocker, insight, decision, meta, failed, anti-pattern, hot-ref, hot-stale -->
<!-- failed = a specific attempt that didn't work (retrospective); anti-pattern = distilled negative rule from one or more failures. Distinct from blocker (something stopping progress now). -->
<!-- hot-ref = observation confirms or applies a claim in hot-memory.md; hot-stale = observation contradicts or supersedes a claim in hot-memory.md. Used to audit hot-memory usefulness. -->
